const WORK_ELEMENTS_HASH_MAP = {
  source: 'source',
  element_id: 'id',
  element_id_type: 'idType',
  type: 'type',
  importance: 'importance',
  utilization: 'utilization',
  required: 'required',
  preferred: 'preferred',
};

export default WORK_ELEMENTS_HASH_MAP;
